---
interface Props {
  text: string;
  word?: string;
}

const { text, word = "misure" } = Astro.props;
---

{
  text.split(new RegExp(`(${word})`, "gi")).map((part) =>
    part.toLowerCase() === word.toLowerCase() ? (
      <strong class="normal-case font-bold">{word}</strong>
    ) : (
      <span>{part}</span>
    )
  )
}
